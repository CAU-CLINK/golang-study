- 블록은 원래 디스크에 분리해서 저장한다. 읽을 때 모두 버퍼에
올리지 않기 위해서. 그러나 여기서는 그냥 함.

- BC(Blockchain)인스턴스는 DB에 저장되어있는
끝 블록의 해쉬값만 가지고 있는다.


-BlockChain(bc) struct는 이제 tip(끝 블록의 해쉬값)과
db포인터로만 이루어져 있다.
->이에 따라 addblock, NewBlockchain 함수들도 바뀐다.
db에 접근해서 해야되니까.
->Iterator가 생긴다. Next()는 현재 이터가 가리키는 블락 리턴


-CLI! 프로그램과 상호 작용할 수 있는 인터페이스를 제공한다!